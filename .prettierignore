# generated by the postinstall hook, and gitignored — but it is still on disk
# when prettier runs, and prettier has no business reformatting a file nobody
# writes by hand
worker-configuration.d.ts

# drizzle's own bookkeeping, likewise generated
migrations/meta

dist
.astro
.wrangler

# agent worktrees — a full checkout of this repo living inside it
.claude
