---
/* the slide faces, loaded only by pages that draw slides */
const slideFonts = [
  "/SourceSerif4Display-Semibold.ttf.woff2",
  "/SourceSerif4-Regular.ttf.woff2",
];
---

{
  slideFonts.map((href) => (
    <link rel="preload" as="font" type="font/woff2" href={href} crossorigin />
  ))
}
